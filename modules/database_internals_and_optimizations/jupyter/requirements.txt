# --- Database Drivers & Tools ---
duckdb==1.1.3              # The Embedded OLAP DB
psycopg2-binary==2.9.9     # The Postgres Driver
ipython-sql==0.5.0         # SQL Magic (%%sql) for Notebooks
SQLAlchemy==2.0.23         # ORM (Pinned to match your environment's era)

# --- Data Generation & Formats ---
Faker==22.5.0              # Data Generator
fastavro==1.12.1           # Avro format handling
pyarrow==14.0.1            # Parquet format handling (Pinned for Py 3.11)

# --- Visualization ---
# (Matplotlib/Seaborn are likely in the base image, but adding here is safe)
matplotlib==3.8.2
seaborn==0.13.0